name: laravel-pint

on:
  pull_request:
  push:
    branches:
      - main
  issue_comment:
    types: [created]

jobs:
  laravel-pint:
    runs-on: ubuntu-latest

    permissions:
      # Give the default GITHUB_TOKEN write permission to commit and push the changed files back to the repository.
      contents: write

    steps:
    - name: Dump GitHub context
      env:
        GITHUB_CONTEXT: ${{ toJson(github) }}
      run: |
        echo "$GITHUB_CONTEXT"

    - name: Dump GitHub context
      if: ${{ github.event.issue.pull_request }}
      run: |
        echo "Running for comment /fix"




    - uses: actions/checkout@v3
      with:
        ref: ${{ github.head_ref }}

    -
       name: Set $FILES
       run: |

         git remote -v
         git branch -v -a
         FILES=$(git diff --diff-filter=d --name-only origin/master | tr '\n' ' '|sed 's/,*$//g')
         echo $FILES

         echo 'FILES<<EOF' >> $GITHUB_ENV
         echo $FILES >> $GITHUB_ENV
         echo 'EOF' >> $GITHUB_ENV

    -
      name: Composer install
      run: composer global require --no-progress -- laravel/pint

    -
      name: Run Laravel Pint
      run: /home/runner/.composer/vendor/bin/pint -v


#    - name: "laravel-pint"
#      uses: aglipanci/laravel-pint-action@2.2.0
#      with:
#        preset: laravel
#        verboseMode: true
#        onlyDirty: true

    #- uses: stefanzweifel/git-auto-commit-action@v4
    #  with:
    #    commit_message: Apply Laravel Pint changes
